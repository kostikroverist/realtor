import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "visitukraine.today",
      "attorneys.ua",
      "blokbud.lviv.ua",
      "cdn.riastatic.com"
    ],
  },
};

export default nextConfig;
